#include <iostream>
#include <vector>
#define FAST_IO ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)

using namespace std;

// n * (m - 1)!



void boj_15649() {
	int n, m;
	cin >> n >> m;

	while (true) {
		vector<int> v;
		
		if (v.size() == m) {
			for (int k : v)
				cout << k << " ";
			cout << endl;

			break;
		}


	}
}