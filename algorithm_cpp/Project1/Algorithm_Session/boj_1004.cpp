#include <iostream>
#include <vector>

using namespace std;

constexpr int MAX = 1001;

void boj_1004() {
	int t;
	cin >> t;

	while (t--) {
		vector<vector<int>> m(MAX, vector<int>(MAX, 0));

		// 출발점과(x1, y1) 도착점(x2, y2)
		int x1, x2, y1, y2;
		cin >> x1 >> y1 >> x2 >> y2;

		int n;
		cin >> n;

		for (int i = 0; i < n; i++) {
			int cx, cy, r;
			cin >> cx >> cy >> r;
		}

		
	}
}